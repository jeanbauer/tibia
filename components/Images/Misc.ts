const path = '/images'

const skills = `${path}/skills.svg`
const background = `${path}/background.gif`
const battle = `${path}/battle.svg`
const chest = `${path}/chest.gif`
const map = `${path}/map.gif`

export { skills, background, chest, battle, map }
